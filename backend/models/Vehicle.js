// Vehicle Detection Model
const Vehicle = {
    schema: {
        type: 'string',
        count: 'number',
        speed_avg: 'number',
        intersectionId: 'string',
        timestamp: 'date'
    }
};

module.exports = Vehicle;
